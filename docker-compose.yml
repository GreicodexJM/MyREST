services:
  mariadb:
    image: mariadb
    environment:
      - MYSQL_ROOT_PASSWORD=toor
    ports:
      - 3307:3306
    volumes:
      - ./tests/sample.sql:/docker-entrypoint-initdb.d/sample.sql
  mysql:
    image: mysql
    environment:
      - MYSQL_ROOT_PASSWORD=toor
    ports:
      - 3306:3306
    volumes:
      - ./tests/sample.sql:/docker-entrypoint-initdb.d/sample.sql
  myrest:
    build: .
    image: greicodex/myrest:latest
    ports:
      - 3000:3000
    environment:
#      - DATABASE_HOST=mysql
#      - DATABASE_USER=root
#      - DATABASE_PASSWORD=toor
#      - DATABASE_NAME=classicmodels
#  # Example using DATABASE_URL with SSL
      - DATABASE_URL=mysql://root:toor@mysql:3306/classicmodels?ssl=false
      # Set JWT_SECRET in your shell or a .env file (never commit real secrets)
      - JWT_SECRET=${JWT_SECRET:-dev_only_secret_do_not_use_in_prod}
      - JWT_REQUIRED=true
    depends_on:
      - mysql
  

